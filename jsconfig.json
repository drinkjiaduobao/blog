{
  "compilerOptions": {
    "target": "es5",
    "module": "esnext",
    "baseUrl": "./",
    "jsx":"preserve",//解决问题栏报
    //TypeScript intellisense is disabled on template. 
    //To enable, configure `"jsx": "preserve"` in the `"compilerOptions"` 
    //property of tsconfig or jsconfig. To disable this prompt instead, 
    //configure `"experimentalDisableTemplateSupport": true` in `"vueCompilerO
    "moduleResolution": "node",
    "paths": {
      "@/*": [
        "src/*"
      ]
    },
    "lib": [
      "esnext",
      "dom",
      "dom.iterable",
      "scripthost"
    ]
  }
}
